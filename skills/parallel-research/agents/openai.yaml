interface:
  display_name: "Parallel Research"
  short_description: "Launch Parallel.ai deep research and write a cited Markdown report."
  default_prompt: "Use $parallel-research to run public-safe Parallel.ai research and write a cited report."
policy:
  allow_implicit_invocation: true
